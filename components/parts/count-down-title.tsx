const CountDownTitle = ({ title }: { title: string }) => {
  return (
    <p className="text-white font-bold font-red-hat uppercase  mt-20">
      {title}
    </p>
  );
};

export default CountDownTitle;
